Clonar el repositorio de github
git clone https://github.com/juanma-/phonecat.git

Añadir los cambios al index
git add . 
git add -A

Subir los cambios al HEAD
git commit -m "Commit message"

Subir los cambios al repositorio remoto
git push origin master

Refrescar el repositorio local 
git pull

Ver los cambios entre el directorio de trabajo, el index (stage) y el HEAD.
git status